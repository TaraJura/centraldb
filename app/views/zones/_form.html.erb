<%= form_with(model: zone) do |form| %>
  <% if zone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(zone.errors.count, "error") %> prohibited this zone from being saved:</h2>

      <ul>
        <% zone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :visitor_id, style: "display: block" %>
    <%= form.number_field :visitor_id %>
  </div>

  <div>
    <%= form.label :customer_id, style: "display: block" %>
    <%= form.select :customer_id, Customer.pluck(:name , :id) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
